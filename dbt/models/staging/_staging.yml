version: 2

models:

  - name: stg_cvm__dre
    description: "DRE unificada (consolidada + individual), transformada a partir do raw CVM"
    columns:
      - name: cd_cvm
        description: "Código CVM da empresa"
        data_tests:
          - not_null

      - name: tipo_df
        description: "Tipo da demonstração financeira: consolidada ou individual"
        data_tests:
          - accepted_values:
              arguments:
                values: ['CONSOLIDADO', 'INDIVIDUAL']

      - name: dt_fim_exercicio
        description: "Data final do período contábil ao qual o valor se refere"
        data_tests:
          - not_null

      - name: valor
        description: "Valor contábil normalizado para reais"
        data_tests:
          - not_null

      - name: ordem_exercicio
        description: "Indica se o registro pertence ao exercício atual ou comparativo"
        data_tests:
          - accepted_values:
              arguments:
                values: ['ÚLTIMO', 'PENÚLTIMO']


  - name: stg_cvm__bpa
    description: "Balanço Patrimonial Ativo unificado (consolidado + individual), transformado a partir do raw CVM"
    columns:
      - name: cd_cvm
        description: "Código CVM da empresa"
        data_tests:
          - not_null

      - name: tipo_df
        description: "Tipo da demonstração financeira: consolidada ou individual"
        data_tests:
          - accepted_values:
              arguments:
                values: ['CONSOLIDADO', 'INDIVIDUAL']

      - name: dt_fim_exercicio
        description: "Data da posição patrimonial"
        data_tests:
          - not_null

      - name: valor
        description: "Valor contábil normalizado para reais"
        data_tests:
          - not_null

      - name: ordem_exercicio
        description: "Indica se o registro pertence ao exercício atual ou comparativo"
        data_tests:
          - accepted_values:
              arguments:
                values: ['ÚLTIMO', 'PENÚLTIMO']


  - name: stg_cvm__bpp
    description: "Balanço Patrimonial Passivo unificado (consolidado + individual), transformado a partir do raw CVM"
    columns:
      - name: cd_cvm
        description: "Código CVM da empresa"
        data_tests:
          - not_null

      - name: tipo_df
        description: "Tipo da demonstração financeira: consolidada ou individual"
        data_tests:
          - accepted_values:
              arguments:
                values: ['CONSOLIDADO', 'INDIVIDUAL']

      - name: dt_fim_exercicio
        description: "Data da posição patrimonial"
        data_tests:
          - not_null

      - name: valor
        description: "Valor contábil normalizado para reais"
        data_tests:
          - not_null

      - name: ordem_exercicio
        description: "Indica se o registro pertence ao exercício atual ou comparativo"
        data_tests:
          - accepted_values:
              arguments:
                values: ['ÚLTIMO', 'PENÚLTIMO']


  - name: stg_cvm__dfc_mi
    description: "DFC pelo método indireto unificada (consolidada + individual), transformada a partir do raw CVM"
    columns:
      - name: cd_cvm
        description: "Código CVM da empresa"
        data_tests:
          - not_null

      - name: tipo_df
        description: "Tipo da demonstração financeira: consolidada ou individual"
        data_tests:
          - accepted_values:
              arguments:
                values: ['CONSOLIDADO', 'INDIVIDUAL']

      - name: dt_fim_exercicio
        description: "Data final do período contábil ao qual o fluxo se refere"
        data_tests:
          - not_null

      - name: valor
        description: "Valor contábil normalizado para reais"
        data_tests:
          - not_null

      - name: ordem_exercicio
        description: "Indica se o registro pertence ao exercício atual ou comparativo"
        data_tests:
          - accepted_values:
              arguments:
                values: ['ÚLTIMO', 'PENÚLTIMO']


  - name: stg_cvm__dfc_md
    description: "DFC pelo método direto unificada (consolidada + individual), transformada a partir do raw CVM"
    columns:
      - name: cd_cvm
        description: "Código CVM da empresa"
        data_tests:
          - not_null

      - name: tipo_df
        description: "Tipo da demonstração financeira: consolidada ou individual"
        data_tests:
          - accepted_values:
              arguments:
                values: ['CONSOLIDADO', 'INDIVIDUAL']

      - name: dt_fim_exercicio
        description: "Data final do período contábil ao qual o fluxo se refere"
        data_tests:
          - not_null

      - name: valor
        description: "Valor contábil normalizado para reais"
        data_tests:
          - not_null

      - name: ordem_exercicio
        description: "Indica se o registro pertence ao exercício atual ou comparativo"
        data_tests:
          - accepted_values:
              arguments:
                values: ['ÚLTIMO', 'PENÚLTIMO']